en:
  activerecord:
    errors:
      models:
        question:
          duplicate_id: Duplicate
        report:
          attributes:
            reason:
              blank: Please select a reason
        user:
          attributes:
            username:
              invalid: can only contain letters, numbers and '_'
              numeric_only: must have a non-number character