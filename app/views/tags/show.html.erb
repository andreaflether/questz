<h4 class="my-4">Questions tagged with <span class="text-orange"><%= @tag %></span></h4>

<div class="row">
  <div class="col-md-9">
    <%= render @questions %>
  </div>
  <div class="col-md-3">
    <div class="row">
      <div class="col-md-12">
        <%= render 'shared/top_users_card', collection: @top_users, tag: @tag, tag_only: true  %>
      </div>
    </div>
  </div>
</div>
