<%= single_content_for :icon do %>
	<i class="fas fa-check"></i> 
<% end %>

<%= single_content_for :action_body do %>
	had a <%= activity.trackable_type.downcase %> accepted
	<% if activity.trackable %>
		on
		<%= link_to activity.trackable.question.title, activity.trackable.question, class: 'fw-bold' %>
	<% else %>
		in a question that no longer exists
	<% end %>
<% end %>
