<%= single_content_for :icon do %>
	<i class="fas fa-question"></i>
<% end %>

<%= single_content_for :action_body do %>
	asked
	<% if activity.trackable %>
		<%= link_to activity.trackable.title, activity.trackable, class: 'fw-bold' %>
	<% else %>
		a <%= activity.trackable_type.downcase %> that no longer exists
	<% end %>
<% end %>