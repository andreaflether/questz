<%= single_content_for :icon do %>
	<i class="fas fa-minus"></i> 
<% end %>
<%= single_content_for :action_body do %>
	unfollowed
	<% if activity.trackable %>
		the tag
		<%= link_to activity.trackable.name, tag_path(activity.trackable), class: 'fw-bold' %>
	<% else %>
		a tag that no longer exists
	<% end %>
<% end %>