<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light border-bottom">
  <div class="container-fluid px-4">
      <ul class="navbar-nav ms-auto">
        <div class="dropdown">
          <a href="#" class="d-flex align-items-center link-dark text-decoration-none dropdown-toggle" id="dropdownUser2" data-bs-toggle="dropdown" aria-expanded="false">
            <% if current_user.avatar? %>
              <%= image_tag(current_user.avatar_url, 
                class: 'rounded-circle user-avatar-45 me-2',
                id: 'user-avatar') %>
            <% else %>
              <%= image_tag 'no-avatar.png',
              class: 'rounded-circle border user-avatar-45 me-2', 
              id: 'user-avatar' %>
            <% end %>
            <%= current_user.username %>
          </a>
          <ul class="dropdown-menu text-center custom-dropdown pb-0" id="admin-info">
            <div class="user-info p-3 bg-primary text-light d-flex flex-column">
              <p>
                <b><%= current_user.name %></b> - <%= t("enums.user.role.#{current_user.role}") %>
              </p>
              <small>Member since <%= format_date(current_user.created_at) %></small>
            </div>
            <ul class="list-group list-group-horizontal-xl">
              <li class="list-group-item list-group-item-action">
                <%= link_to edit_user_registration_path do %>
                  Edit info
                <% end %>
              </li>
              <li class="list-group-item list-group-item-action">
                <%= link_to destroy_user_session_path do %>
                  Sign out
                <% end %>
              </li>
            </ul>
          </ul>
        </div>
      </ul>
  </div>
</nav>