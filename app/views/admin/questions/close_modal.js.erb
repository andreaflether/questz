var closeModal = new bootstrap.Modal(document.getElementById('modal-close'))

$('#modal-close-content').html("<%= j render 'admin/questions/close_question_form', question: @question %>")

DependentFields.bind()
closeModal.show()