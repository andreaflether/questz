<h2 class="text-center mb-3">Mudar senha</h2>

<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.input :reset_password_token, as: :hidden %>

  <div class="form-inputs">
    <%= f.input :password,
                label: "Nova senha",
                required: true,
                autofocus: true,
                hint: ("Mínimo: #{@minimum_password_length} caracteres" if @minimum_password_length),
                input_html: { autocomplete: "new-password", placeholder: "Digite a nova senha" },
                wrapper_html: { class: 'has-float-label'} %>
    <%= f.input :password_confirmation,
                label: "Confirmação da nova senha",
                required: true,
                input_html: { autocomplete: "new-password", placeholder: "Confirme sua nova senha" },
                wrapper_html: { class: 'has-float-label'} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Confirmar mudança", class: 'btn btn-orange btn-block' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
